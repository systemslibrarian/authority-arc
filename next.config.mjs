/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The site is hosted on Vercel with a custom domain via CNAME (see README).
  // No special asset prefix or trailing slash handling needed.
};

export default nextConfig;
